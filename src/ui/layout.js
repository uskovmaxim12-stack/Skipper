import{getState}from'../core/state.js';import{esc,initials}from'../core/utils.js';
const items=[['home','⌂','Главная'],['feed','▤','Лента'],['forum','◈','Форум'],['crew','♟','Экипаж'],['messages','✉','Чаты'],['games','✦','Игры']];
export function shell(content){const s=getState(),u=s.user,unread=s.notifications.filter(n=>!n.read).length;return `<div class="app-shell"><header class="topbar"><div class="brand"><div class="brand-mark">🐧</div><div><b>Шкипер</b><small>авиационная экосистема · v${window.SKIPPER_VERSION||'12'}</small></div></div><div class="top-actions"><button class="icon-btn" id="themeBtn" aria-label="Тема">☼</button><button class="icon-btn notify" data-notifications aria-label="Уведомления">◉${unread?`<i>${unread>99?'99+':unread}</i>`:''}</button>${u?`<button class="profile-mini" data-route="profile"><span class="avatar">${u.photo?`<img src="${esc(u.photo)}">`:initials(u.name)}</span><span class="profile-mini-text"><b>${esc(u.name)}</b><small>${esc(u.role)}</small></span></button>`:`<button class="login-mini" data-login>Войти</button>`}</div></header><main class="content">${content}</main><nav class="bottom-nav">${items.map(([r,ic,t])=>`<button data-route="${r}" class="nav-item ${s.route===r?'active':''}"><span>${ic}</span><small>${t}</small></button>`).join('')}<button data-route="profile" class="nav-item ${s.route==='profile'?'active':''}"><span>◌</span><small>Профиль</small></button></nav></div>`}
